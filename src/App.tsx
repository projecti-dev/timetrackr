import { Typography, Box } from "@mui/material";

const App = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4" color="primary">
        TimeTrackr
      </Typography>
    </Box>
  );
};

export default App;
